// 默认dev配置
const config = {
    DEBUG: false,// 是否调试
    // MYSQL数据库连接配置
    MYSQL:{
        host: 'localhost',
        database: 'zsb.e21.cn',
        username:'root',
        password: ''
    },
    host: 'localhost',
    port: 4000
}
module.exports = config
